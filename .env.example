# =============================================================================
# Configuración de Entorno - Retail ETL Project
# INSTRUCCIONES:
# 1. Copia este archivo a .env en el mismo directorio
# 2. Actualiza los valores según tu configuración local
# 3. NUNCA subas el archivo .env a Git (contiene credenciales)
# =============================================================================

# POSTGRESQL CONFIGURATION
# ========================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres_password_123
POSTGRES_DB=retail_db
POSTGRES_PORT=5432

# DOCKER CONFIGURATION
# ====================
# Nombre del contenedor
DOCKER_CONTAINER_NAME=retail_pg_db

# Imagen de PostgreSQL (usar versión estable)
POSTGRES_IMAGE=postgres:16-alpine

# VOLÚMENES
# =========
# Ruta local para almacenar datos de la BD (persistencia)
POSTGRES_DATA_VOLUME=./data/postgres_data

# CONEXIÓN REMOTA (si planeas acceder desde fuera del contenedor)
# ===============================================================
# Máquina host
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres_password_123
DB_NAME=retail_db
